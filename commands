-- init
1. docker-compose up -d

-- mysql
docker exec -it <container id> /bin/bash


-- docker
 docker-compose -p web-nginx-react -f docker-compose.yml ps
 docker-compose -p node-server-blue -f docker-compose.green.yml ps
 docker-compose -p node-server-blue -f docker-compose.blue.yml ps

 